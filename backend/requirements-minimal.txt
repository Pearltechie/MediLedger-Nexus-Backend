# MediLedger Nexus - Minimal Requirements for Free Tier Deployment
# Optimized for Render.com free tier (512MB RAM, 0.1 vCPU)

# Web Framework (Essential)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Data Validation (Essential)
pydantic>=2.5.0
python-multipart>=0.0.6

# Authentication & Security (Essential)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=41.0.0

# Environment & Configuration (Essential)
python-dotenv>=1.0.0

# File Handling (Essential)
aiofiles>=23.2.1

# HTTP Client (Essential)
httpx>=0.25.0

# Database (Lightweight)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0  # SQLite for free tier

# Caching (Optional - can be disabled)
# redis>=5.0.0  # Commented out for free tier
# celery>=5.3.0  # Commented out for free tier

# Blockchain (Essential for Hedera)
# Note: Hedera SDK will be added when available
# hedera-sdk-py>=2.0.0  # Add when available

# AI & Machine Learning (DISABLED for free tier)
# torch>=2.1.0  # Too heavy for free tier
# numpy>=1.24.0  # Keep minimal version if needed
# pandas>=2.1.0  # Too heavy for free tier
# scikit-learn>=1.3.0  # Too heavy for free tier

# Groq AI API (Lightweight - keep for basic AI)
groq>=0.4.1

# JSON Logging (Essential)
python-json-logger>=2.0.7

# Additional utilities (Essential)
click>=8.1.0
rich>=13.0.0

# Development Dependencies (Optional for production)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# isort>=5.12.0

# Minimal NumPy for basic operations (if needed)
numpy>=1.24.0

# Note: Total estimated size: ~50-100MB (well under 512MB limit)
# Note: Remove ML dependencies to stay within free tier limits
